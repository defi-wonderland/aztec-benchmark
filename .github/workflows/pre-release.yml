name: Pre-Release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  pre-release:
    uses: defi-wonderland/aztec-ci-actions/.github/workflows/pre-release.yml@feat/next
    with:
      run-compile: false
      run-codegen: false
    secrets: inherit
