name: Pre-Release

on:
  pull_request: # TODO: remove this after testing
  workflow_dispatch:

permissions:
  contents: write

jobs:
  pre-release:
    uses: defi-wonderland/aztec-ci-actions/.github/workflows/pre-release.yml@v0
    with:
      run-compile: false
      run-codegen: false
    secrets: inherit
